<Window x:Class="PUBGMortar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PUBGMortar.ViewModels"
        mc:Ignorable="d"
        Title="PUBG Hỗ trợ cối"
        SizeToContent="Height"
        Width="360"
        MinHeight="400"
        MinWidth="340"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        d:DataContext="{d:DesignInstance vm:MainViewModel}">

    <Window.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E8AD4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#006CBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4D4D4D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2D2D2D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BadgeStyle" TargetType="Border">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <!-- Tiêu đề -->
            <TextBlock Text="🎯 PUBG Hỗ trợ cối"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,16"/>

            <!-- Hiển thị trạng thái -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Trạng thái đo" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                        <Border Style="{StaticResource BadgeStyle}" HorizontalAlignment="Right"
                                Background="{Binding IsListening, Converter={StaticResource BoolToBrushConverter}}">
                            <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="11"/>
                        </Border>
                    </Grid>

                    <!-- Kết quả chính -->
                    <StackPanel Margin="0,16,0,0">
                        <TextBlock Text="Khoảng cách đặt cối" FontSize="11" Foreground="#888"/>
                        <TextBlock Text="{Binding ResultText}"
                                   FontSize="36"
                                   FontWeight="Bold"
                                   Foreground="#0078D4"/>
                    </StackPanel>

                    <!-- Dữ liệu chi tiết -->
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Background="#252525" CornerRadius="6" Padding="12,8">
                            <StackPanel>
                                <TextBlock Text="Khoảng cách ngang" FontSize="10" Foreground="#666"/>
                                <TextBlock Text="{Binding HorizontalDistanceText}"
                                           FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Background="#252525" CornerRadius="6" Padding="12,8">
                            <StackPanel>
                                <TextBlock Text="Góc nâng" FontSize="10" Foreground="#666"/>
                                <TextBlock Text="{Binding ElevationAngleText}"
                                           FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Nút điều khiển -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,16">
                <Button Content="{Binding IsListening, Converter={StaticResource BoolToListenTextConverter}}"
                        Command="{Binding ToggleListeningCommand}"
                        Style="{StaticResource AccentButton}"/>
                <Button Content="Đặt lại"
                        Command="{Binding ResetMeasurementCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Margin="12,0,0,0"/>
            </StackPanel>

            <!-- Hướng dẫn sử dụng -->
            <Border Style="{StaticResource CardStyle}" Margin="0">
                <StackPanel>
                    <TextBlock Text="Phím tắt" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="#00A86B" CornerRadius="4" Padding="8,4" Margin="0,0,12,0">
                            <TextBlock Text="Alt+Q" Foreground="White" FontSize="11" FontWeight="Bold"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Đo / đóng thông báo" Foreground="White" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="#D4A000" CornerRadius="4" Padding="8,4" Margin="0,0,12,0">
                            <TextBlock Text="Ctrl+Alt+Q" Foreground="Black" FontSize="11" FontWeight="Bold"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Đặt lại tỉ lệ và đo" Foreground="White" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="#D4A000" CornerRadius="4" Padding="8,4" Margin="0,0,12,0">
                            <TextBlock Text="Alt+Chuột trái" Foreground="Black" FontSize="11" FontWeight="Bold"/>
                        </Border>
                        <TextBlock Grid.Column="1" Text="Đặt điểm đo" Foreground="White" VerticalAlignment="Center"/>
                    </Grid>

                    <TextBlock Text="Các bước đo:" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,4"/>
                    <TextBlock TextWrapping="Wrap" Foreground="#AAA" FontSize="11" LineHeight="18"
                               Text="1. Alt+Q để bắt đầu đo (đang có thông báo thì đóng lại)&#x0a;2. Lần đầu tự động đặt tỉ lệ 100m&#x0a;3. Click vị trí của bạn rồi vị trí mục tiêu&#x0a;4. Ngắm mục tiêu rồi click để lấy góc nâng&#x0a;5. Xem kết quả để chỉnh cối&#x0a;6. Alt+Q để đóng thông báo hoặc đo lượt tiếp&#x0a;7. Ctrl+Alt+Q để đặt lại tỉ lệ"/>
                </StackPanel>
            </Border>

            <!-- Thông tin tác giả -->
            <TextBlock HorizontalAlignment="Center" Margin="0,8,0,0">
                <Hyperlink NavigateUri="https://zhaiyiming.com/feedback"
                           RequestNavigate="Hyperlink_RequestNavigate"
                           Foreground="#888"
                           TextDecorations="None">
                    <Hyperlink.Style>
                        <Style TargetType="Hyperlink">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#0078D4"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Hyperlink.Style>
                    Tác giả: 茗伊 @tinymins - &lt;root@zhaiyiming.com&gt;
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
